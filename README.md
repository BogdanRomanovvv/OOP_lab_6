# Редактор подземелья Balagur Fate 3

Лабораторная работа №6 по теме "Паттерны проектирования"

## Описание

Редактор подземелья для RPG игры, позволяющий:
- Добавлять NPC (Странствующие рыцари, Друиды, Эльфы)
- Сохранять и загружать локации из файла
- Просматривать список NPC
- Запускать боевой режим с заданной дальностью

## Использованные паттерны

1. **Factory** - для создания NPC и загрузки из файла
2. **Visitor** - для реализации боевой системы
3. **Observer** - для логирования событий (консоль + файл log.txt)

## Правила боя

- Рыцарь убивает Эльфа
- Эльф убивает Друида и Рыцаря
- Друид убивает Друида

## Компиляция

### Windows (MinGW/MSVC)
```bash
mkdir build
cd build
cmake ..
cmake --build .
```

### Linux/Mac
```bash
mkdir build
cd build
cmake ..
make
```

### Docker 🐳
```bash
# Сборка образа
docker build -t dungeon-editor .

# Запуск программы
docker run -it --rm dungeon-editor

# Запуск тестов
docker run --rm dungeon-editor ./dungeon_tests

# С Docker Compose
docker-compose up dungeon-editor
```

**Детали:** `DOCKER.md`

### Компиляция без тестов
```bash
cmake .. -DBUILD_TESTS=OFF
cmake --build .
```

## Запуск

### Основная программа
```bash
./dungeon_editor  # Linux/Mac
dungeon_editor.exe  # Windows
```

### Тестирование

#### Запуск всех тестов
```bash
cd build
ctest --output-on-failure
# или напрямую
./dungeon_tests.exe  # Windows
./dungeon_tests      # Linux/Mac
```

#### Запуск конкретного теста
```bash
./dungeon_tests --gtest_filter=NPCFactoryTest.*
```

#### Статистика по тестам
Всего **29 тестов** покрывающих:
- Factory Pattern (6 тестов)
- Вычисление расстояний (3 теста)
- Боевую систему (6 тестов)
- Observer Pattern (2 теста)
- DungeonEditor (10 тестов)
- Сериализацию (2 теста)